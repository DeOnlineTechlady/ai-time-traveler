export async function generateTimeTravelImage(base64Image, decade) {
  // Placeholder: return same image and a description.
  return {
    url: base64Image,
    description: 'Placeholder time travel result for ' + decade + '.'
  };
}
